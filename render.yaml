services:
  - type: web
    name: neurasticity-brainflow
    runtime: python
    plan: free
    buildCommand: pip install .
    startCommand: uvicorn brainflow_service.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
